# dependencies
node_modules

# build outputs (regenerated by build-binary.sh / slidev build)
dist
dist-single
payload.tgz
coverage
.node-bin
bin/cache-key.ts

# per-request temp from the deck server
.jobs
.deck-*.md

# local env (use .env.example as the template)
.env
.env.*
!.env.example

# misc
*-export.*
.DS_Store
.claude/
